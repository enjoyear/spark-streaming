package com.chen.guo

/**
  * Ref:
  * https://spark.apache.org/docs/latest/structured-streaming-kafka-integration.html
  *
  * Reads data from Kafka. It’s compatible with Kafka broker versions 0.10.0 or higher.
  */
class StreamKafka {

  //  private val frame: DataFrame = new SparkSession(ssc.sparkContext).readStream.format("kafka")
  //    .option("kafka.boostrap.servers", "abc")
  //    .option("subscribe", "topic")
  //    .load()
  //  frame.select().selectExpr()


}
